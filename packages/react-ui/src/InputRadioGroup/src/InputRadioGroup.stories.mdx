import {
  ArgsTable,
  Meta,
  Story,
  Canvas,
  Source,
  Preview,
} from "@storybook/addon-docs/blocks";

import InputRadioGroup from "./InputRadioGroup";
import InputRadio from "../../InputRadio/src/";

<!-- Set up radio input groups for group children prop -->

export const RadioGroup1 = () => (
  <>
    <InputRadio id="radio-1" name="radio-test" label="Radio 1" value={1} />
    <InputRadio id="radio-2" name="radio-test" label="Radio 2" value={2} />
    <InputRadio id="radio-3" name="radio-test" label="Radio 3" value={3} />
  </>
);

export const RadioGroup2 = () => (
  <>
    <InputRadio id="radio-4" name="radio-test" label="Radio 4" value={4} />
    <InputRadio id="radio-5" name="radio-test" label="Radio 5" value={5} />
    <InputRadio id="radio-6" name="radio-test" label="Radio 6" value={6} />
  </>
);

<!-- Set up error messages for error stories -->

export const allErrors = {
  day: "The day you entered is invalid",
  month: "The month you entered is invalid",
  year: "The year you entered is invalid",
};

<!-- Add base Meta for each story,
     meta like args can be overridden at story level if required -->

<Meta
  title="Elements/InputRadioGroup"
  component={InputRadioGroup}
  subcomponents={InputRadioGroup}
/>

<!--- Start content -->

# InputRadioGroup

Intro to InputRadioGroup.

## Usage

Usage of radio form controls

## Default InputRadioGroup example

A test canvas to show the component and it's underlying code.
A canvas can include stories or just plain components like the one below.

<Canvas>
  <InputRadioGroup
    id="group1"
    legend="Basic radio group with hint"
    hint="Hint message to help guide the user"
  >
    <InputRadio id="radio-1" name="radio-test" label="Radio 1" value={1} />
    <InputRadio id="radio-2" name="radio-test" label="Radio 2" value={2} />
    <InputRadio id="radio-3" name="radio-test" label="Radio 3" value={3} />
  </InputRadioGroup>
</Canvas>

## Props

<p>The InputRadio takes the props from the table below.</p>
<ArgsTable components={{ InputRadio: InputRadio }} />

<hr />

## Stories for the InputRadio component

We can keep component stories at the bottom of the documentation pages, for better doc readability.

### Basic InputRadioGroup story

<Canvas>
  <Story
    name="Basic"
    args={{
      id: "group-1",
      legend: "Radio Group with hint",
      hint: "Hint message",
      children: <RadioGroup1 />,
    }}
  >
    {(args) => <InputRadioGroup {...args} />}
  </Story>
</Canvas>

### Basic InputRadioGroup story

<Canvas>
  <Story
    name="With errors"
    args={{
      id: "group-3",
      legend: "Radio Group with hint and error",
      hint: "Hint message",
      errorMsg: "Error message",
      hasError: true,
      children: <RadioGroup1 />,
    }}
  >
    {(args) => <InputRadioGroup {...args} />}
  </Story>
</Canvas>
