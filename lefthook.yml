pre-commit:
  parallel: true
  commands:
    build-packages:
      tags: frontend css
      glob: "*.{css,pcss}"
      exclude: packages/foundations-vars/**
      run: lerna run dist && git add {staged_files}
    build-tokens:
      tags: frontend css
      glob: "packages/foundations-vars/*.{js}"
      run: node ./packages/foundations-vars/build.js && node ./packages/foundations-vars/merge.js